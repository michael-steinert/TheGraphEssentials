{
  "name": "flipcoin",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ michael-steinert>/flipcoin",
    "create-local": "graph create --node http://localhost:8020/ michael-steinert>/flipcoin",
    "remove-local": "graph remove --node http://localhost:8020/ michael-steinert>/flipcoin",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 michael-steinert>/flipcoin"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
